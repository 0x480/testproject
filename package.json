{
  "name": "my-next-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "yarn prisma generate && yarn prisma migrate dev",
    "dev": "yarn prisma generate && yarn prisma migrate dev && next dev",
    "build": "yarn prisma generate && yarn prisma migrate dev && next build",
    "start": "yarn prisma generate && yarn prisma migrate dev && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "jotai": "^2.12.1",
    "next": "14.2.24",
    "primereact": "^10.9.2",
    "prisma": "^5.22.0",
    "react": "^18",
    "react-dom": "^18",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.24",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
